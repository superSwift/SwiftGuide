# A股量化系统落地方案（界面 + 数据接口 + 数据库 + 选股模型）

> 面向“不懂编程但有策略想法”的使用者，目标是先跑通，再逐步增强。

## 1. 目标与边界

- **市场**：A 股（沪深主板 + 创业板 + 科创板，可配置）
- **功能**：
  - 可视化策略编辑（规则拼装 + 参数配置）
  - 数据接口统一管理（行情、财务、指数、行业）
  - 数据库落地与缓存
  - 选股模型执行（定时选股 + 回测 + 结果查看）
  - 基础风控与日志审计
- **不做**：自动下单（第一阶段先不碰券商交易接口）

---

## 2. 推荐开源技术栈（优先可落地）

### 2.1 前端（策略编辑界面）

- **Node-RED**（低代码流程编排，拖拽式）
  - 优点：无需前端开发基础，能快速搭建“策略流程”
  - 适合你：通过节点配置条件和执行逻辑
- 备选：**Streamlit**（Python 快速搭界面，非拖拽但简单）

### 2.2 后端服务

- **FastAPI**
  - 负责策略管理、任务调度、数据查询 API
  - 文档自动生成，便于后续扩展

### 2.3 数据接口

- **AkShare**（优先）
  - 覆盖 A 股常用行情与财务字段，开源生态活跃
- 备选：Tushare（部分能力更强但积分限制）

### 2.4 数据库

- **PostgreSQL + TimescaleDB（可选）**
  - 存储日线行情、因子数据、回测结果
- **Redis**
  - 缓存热点数据、任务状态

### 2.5 回测与策略执行

- **Backtrader**（成熟、稳定）
  - 支持策略参数化、绩效指标、调仓逻辑

### 2.6 调度与异步任务

- **APScheduler** 或 **Celery + Redis**
  - 每日收盘后自动更新数据并执行选股

### 2.7 可视化监控

- **Metabase**（零代码 BI）
  - 看策略收益曲线、胜率、回撤

---

## 3. 系统架构（从简单到完整）

```text
[Node-RED/Streamlit 策略界面]
             |
             v
        [FastAPI 网关]
      /         |         \
     v          v          v
[数据服务]   [策略服务]   [回测服务]
     |          |          |
     v          v          v
 [AkShare]  [策略规则库]  [Backtrader]
     |                       |
     +----------> [PostgreSQL] <----+
                     ^                |
                     |                v
                  [Redis]        [Metabase]
```

---

## 4. 最小可用版本（MVP）建议（4 周）

### 第 1 周：基础设施

1. Docker Compose 起服务：PostgreSQL、Redis、FastAPI、Metabase
2. 定义数据库表：
   - `stock_daily`（行情）
   - `stock_financial`（财务）
   - `strategy_config`（策略参数）
   - `selection_result`（选股结果）

### 第 2 周：数据打通

1. 用 AkShare 拉取基础日线 + 财务指标
2. 写入 PostgreSQL
3. 提供 FastAPI 查询接口（按日期/股票查询）

### 第 3 周：策略执行

1. 先支持“规则型选股”（非机器学习）：
   - 例：PE 区间、ROE 阈值、均线条件
2. 输出每日候选池 + 评分
3. 接入 Backtrader 做历史回测

### 第 4 周：界面与自动化

1. Node-RED/Streamlit 配置策略参数
2. APScheduler 每日定时运行
3. Metabase 展示选股与回测结果

---

## 5. 关键数据表设计（简化）

### `strategy_config`

- `id`
- `name`
- `market_scope`
- `rebalance_freq`（daily/weekly/monthly）
- `rules_json`（规则配置）
- `risk_json`（风控配置）
- `is_active`
- `created_at`

### `selection_result`

- `id`
- `strategy_id`
- `trade_date`
- `symbol`
- `score`
- `reason`
- `created_at`

---

## 6. “不会编程”也能用的交互方式

建议把策略拆成 5 类可配置项（界面表单化）：

1. **股票池过滤**：ST、上市天数、板块范围
2. **基本面规则**：PE、PB、ROE、营收增速
3. **技术面规则**：MA 金叉、成交量放大、突破
4. **调仓规则**：周频/月频、等权/打分加权
5. **风控规则**：止损、最大回撤、单票仓位

这样你只需要调参数，不需要直接写代码。

---

## 7. 典型风险与规避

- **数据口径不一致**：统一字段字典与复权规则
- **未来函数/偷看偏差**：回测时仅使用当日可得数据
- **交易成本忽略**：必须计入手续费、滑点、印花税
- **过拟合**：样本外验证 + 滚动回测

---

## 8. 你现在可以马上做的事

1. 先确定你的策略规则（我可以帮你把口语转成规则 JSON）
2. 决定是否先用“周频选股 + 不自动下单”
3. 我可继续给你：
   - 一份可直接运行的 `docker-compose` 草案
   - 策略参数模板（表单字段定义）
   - 第一版 API 列表（增删改查 + 执行回测）

---

## 9. 推荐实施策略（现实可行）

- **阶段 1（1~2 个月）**：规则策略 + 回测 + 可视化
- **阶段 2（2~4 个月）**：因子评分、多策略组合
- **阶段 3（4 个月+）**：机器学习模型、自动风控、半自动交易

先把“可解释、可复现、可迭代”跑起来，比一开始追求复杂模型更重要。
